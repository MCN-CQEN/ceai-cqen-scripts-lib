name: "Desinstaller une application dans ArgoCD"
description: "Desinstaller une application dans ArgoCD si elle existe"

inputs:
  argocd_server:
    description: "URL du serveur Argo CD"
    required: true
  argocd_version:
    description: "La version d'argocd"
    default: "v2.10.20" # current version of argocd for the lab (202511)
  argocd_username:
    description: "Nom d'utilisateur Argo CD"
    default: "admin"
  argocd_password:
    description: "'Mot de passe de l'utilisateur Argo CD"
    required: true
  app_project_name:
    description: "Le nom du projet où l'application est déployée"
    required: true
  app_name:
    description: "Le nom de l'application"
    required: true
  app_dest_namespace: 
    description: "Namespace Kubernetes de destination de l'application"
    default: "defaultApp"
  delete_persistent_volume_claims:
    description: "Supprimer explicitement les PVC de l'application avant la suppression Argo CD"
    default: "false"

runs:
  using: "composite"
  steps:    

    - name: Vérifier et installer ArgoCD CLI
      shell: bash    
      run: |
        if ! command -v argocd &> /dev/null
        then
            echo "ArgoCD CLI not found, installing..."
            wget https://github.com/argoproj/argo-cd/releases/download/${{inputs.argocd_version}}/argocd-linux-amd64
            sudo install -m 555 argocd-linux-amd64 /usr/local/bin/argocd
            rm argocd-linux-amd64
            echo "ArgoCD CLI installed."
        else
            echo "ArgoCD CLI is already installed."
        fi
        argocd version --client # Verify installation

    - name: Login au Argo CD
      shell: bash
      run: |
        set -e

        echo "Connexion au serveur Argo CD : ${{ inputs.argocd_server }}"

        argocd login "${{ inputs.argocd_server }}" --username "${{ inputs.argocd_username }}" --password "${{ inputs.argocd_password }}" --grpc-web --insecure
        # Capture the exit code of the last command
        if [ $? -ne 0 ]; then
          echo "ArgoCD login failed."
          echo "ERREUR : Impossible de se connecter à Argo CD."
          echo "Causes possibles :"
          echo "   - serveur Argo CD éteint"
          echo "   - URL incorrecte"
          echo "   - problème réseau"
          echo "   - le cluster n’existe pas"
          exit 1          
        else
          echo "Connexion Argo CD réussie"
        fi

    - name: Check si le projet Argo CD existe
      id: check_project
      continue-on-error: true # This ensures the step won't fail the workflow if the command errors
      shell: bash
      run: |
        if argocd proj get ${{ inputs.app_project_name }}; then
          echo "project_exists=true" >> $GITHUB_OUTPUT
        else
          echo "project_exists=false" >> $GITHUB_OUTPUT
        fi 
    
    - name: Print résultat de la vérification si le projet existe
      shell: bash
      run: |
        if ${{steps.check_project.outputs.project_exists == 'false'}}; then
          echo "Projet ${{inputs.app_project_name}} does not exist, exit workflow. Contactez l'administrateur de projets pour créer le projet"
        else
          echo "Project ${{inputs.app_project_name}} exists, will continue"
        fi

    - name: Check if Argo CD application exists and remove if exists
      if: steps.check_project.outputs.project_exists == 'true'
      shell: bash
      run: |
        set -euo pipefail

        APP_NAME="${{ inputs.app_name }}"
        APP_DEST_NAMESPACE="${{ inputs.app_dest_namespace }}"
        APP_PROJECT="${{ inputs.app_project_name }}"
        DELETE_PVC="${{ inputs.delete_persistent_volume_claims }}"
        ARGOCD_USERNAME="${{ inputs.argocd_username }}"
        APP_LABEL="$APP_NAME"
        
        echo "Vérification de l'existence de l'application Argo CD : $APP_LABEL"

        # allow the `argocd app get` to fail without terminating the whole script
        set +e
        APP_GET_STATUS=0
        APP_GET_OUTPUT="$(argocd app get "$APP_NAME" --grpc-web 2>&1)" || APP_GET_STATUS=$?
        APP_GET_STATUS=${APP_GET_STATUS:-0}
        set -e

        if [ $APP_GET_STATUS -eq 0 ]; then
          printf '%s\n' "$APP_GET_OUTPUT"
          echo "Application '$APP_LABEL' trouvée. Suppression en cours ..."

          if [ "$DELETE_PVC" = "true" ]; then
            echo "Suppression des annotations de préservation pour l'application '$APP_NAME'..."

            # Tentative via ArgoCD (merge-patch idempotent — met la valeur à null si présente)
            if argocd app patch-resource "$APP_NAME" \
                --project "$APP_PROJECT" \
                --kind PersistentVolumeClaim \
                --namespace "$APP_DEST_NAMESPACE" \
                --all \
                --patch '{"metadata":{"annotations":{"helm.sh/resource-policy":null,"argocd.argoproj.io/sync-options":null}}}' \
                --patch-type application/merge-patch+json \
                --grpc-web; then
              echo "Annotations mises à jour via argocd."
            else
              echo "argocd patch-resource a échoué — tentative de fallback via kubectl (suppression d'annotations)..."
              # Supprime les annotations si présentes (syntax: key- supprime l'annotation)
              kubectl -n "$APP_DEST_NAMESPACE" annotate pvc -l app.kubernetes.io/instance="$APP_NAME" helm.sh/resource-policy- argocd.argoproj.io/sync-options- --overwrite || true
              echo "Fallback kubectl terminé (annotations supprimées si elles existaient)."
            fi

            # Forcer une actualisation Argo CD pour prendre en compte la modification avant suppression
            echo "Forcer un refresh ArgoCD de l'application..."
            argocd app refresh "$APP_NAME" --grpc-web || true

            # (Optionnel) attente courte pour laisser le refresh se propager
            sleep 2

          fi

          # attempt deletion; propagate a real delete failure
          argocd app delete "$APP_NAME" --cascade --yes --grpc-web
          echo "Application '$APP_LABEL' supprimée avec succès"
        else
          # Print argocd output for debugging
          printf '%s\n' "$APP_GET_OUTPUT"

          # If output indicates the app doesn't exist, succeed gracefully
          if printf '%s\n' "$APP_GET_OUTPUT" | grep -Eiq "code = NotFound|not found|does not exist|n.existe pas"; then
            echo "Application '$APP_LABEL' n'existe pas. Rien à supprimer."
            exit 0
          fi

          # If output indicates permission issues, fail with an actionable message
          if printf '%s\n' "$APP_GET_OUTPUT" | grep -Eiq "code = PermissionDenied|permission denied|PermissionDenied"; then
            echo "::error::Permission denied while reading Argo CD application '$APP_LABEL'."
            echo "Vérifiez :"
            echo "  - Les identifiants ($ARGOCD_USERNAME) / token fournis sont corrects"
            echo "  - Le compte a les droits RBAC nécessaires pour lire/effacer l'application (applications.get / applications.delete)"
            echo "  - La configuration du serveur argocd (TLS / grpc-web / --insecure) est correcte"
            echo "Exemple: retenter 'argocd login' puis 'argocd app get $APP_NAME' localement pour reproduire."
            exit 2
          fi

          # Otherwise treat as a real error
          echo "::error::Impossible de lire l'application Argo CD '$APP_LABEL'."
          exit $APP_GET_STATUS
        fi
